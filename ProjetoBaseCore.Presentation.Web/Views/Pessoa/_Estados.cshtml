@using ProjetoBaseCore.Application.ViewModels
@model PessoaViewModel
@{
    var estados = (IEnumerable<SelectListItem>)new EnderecoViewModel().Estados();
}
<select class="form-control" asp-for="Endereco.Estado" asp-items="@estados">
    <option value="">Selecione</option>
</select>   